# Copy this file to `.env` (which is gitignored) before running `astro dev start`.
# DuckDB needs no credentials, but the path is still externalized as a matter
# of good habit — see profiles.yml for why.
DUCKDB_PATH=/usr/local/airflow/include/dbt/ecommerce/dbt.duckdb